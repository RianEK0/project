import { FeaturePlaceholder } from '@/features/dashboard/feature-placeholder';

export default function SuperAdminSystemSettingsPage() {
  return (
    <FeaturePlaceholder
      title="System Settings"
      description="Registration, maintenance mode, and platform defaults will be configured here."
    />
  );
}

