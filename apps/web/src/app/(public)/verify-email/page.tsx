import { AuthPlaceholderPage } from '@/features/auth/auth-placeholder-page';

export default function VerifyEmailPage() {
  return (
    <AuthPlaceholderPage
      title="Verify Email"
      description="Verification links sent from the backend will land on this route."
    />
  );
}

