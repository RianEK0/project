import { AuthPlaceholderPage } from '@/features/auth/auth-placeholder-page';

export default function RegisterPage() {
  return (
    <AuthPlaceholderPage
      title="Register"
      description="The registration flow will be wired to the backend auth module in the authentication phase."
    />
  );
}

