import { AuthPlaceholderPage } from '@/features/auth/auth-placeholder-page';

export default function ResetPasswordPage() {
  return (
    <AuthPlaceholderPage
      title="Reset Password"
      description="Token verification and password update flow will be available after the auth module is connected."
    />
  );
}

