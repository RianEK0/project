import { FeaturePlaceholder } from '@/features/dashboard/feature-placeholder';

export default function RolesPage() {
  return (
    <FeaturePlaceholder
      title="Roles"
      description="Custom roles, permission matrix management, and system role safeguards will live here."
    />
  );
}

