import { FeaturePlaceholder } from '@/features/dashboard/feature-placeholder';

export default function WorkspacesPage() {
  return (
    <FeaturePlaceholder
      title="Workspaces"
      description="Each organization will manage active and archived workspaces from this area."
    />
  );
}

