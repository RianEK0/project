import { FeaturePlaceholder } from '@/features/dashboard/feature-placeholder';

export default function ProfilePage() {
  return (
    <FeaturePlaceholder
      title="Profile"
      description="Profile update, password change, preferences, and active sessions will be shown here."
    />
  );
}

