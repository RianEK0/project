import { FeaturePlaceholder } from '@/features/dashboard/feature-placeholder';

export default function OrganizationPage() {
  return (
    <FeaturePlaceholder
      title="Organization"
      description="Company profile, settings, and archive workflow will be connected to the organizations module."
    />
  );
}

