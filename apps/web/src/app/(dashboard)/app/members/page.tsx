import { FeaturePlaceholder } from '@/features/dashboard/feature-placeholder';

export default function MembersPage() {
  return (
    <FeaturePlaceholder
      title="Members"
      description="Search, filters, pagination, invitation, and role assignment will be wired to membership endpoints."
    />
  );
}

