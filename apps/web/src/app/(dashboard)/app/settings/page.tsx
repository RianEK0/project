import { FeaturePlaceholder } from '@/features/dashboard/feature-placeholder';

export default function SettingsPage() {
  return (
    <FeaturePlaceholder
      title="Settings"
      description="Organization settings and user preferences will be managed from this workspace."
    />
  );
}

